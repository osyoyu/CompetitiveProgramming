#include <iostream>
#include <vector>
#include <utility>

using namespace std;

vector<int> parent;
vector<int> rank;

int main() {
  int n, m;
  cin >> n >> m;

  for (int i = 0; i < m; i++) {
    int a, b;
    cin >> a >> b;

    E.push_back(pair<int, int>(a, b));

  }

  int answer = 0;

  return 0;
}
